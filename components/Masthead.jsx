import React from "react";

const Masthead = (props) => {
    console.log(props);

    return (
        <header>
            <h1>This is the masthead</h1>
        </header>
    )
}

export default Masthead;